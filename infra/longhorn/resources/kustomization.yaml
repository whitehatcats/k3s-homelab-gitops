# ============================================================
# Kustomization — Longhorn Dashboard + S3 Sync
# ============================================================

resources:
  # ------------------------------------------------------------
  # Access and Identity
  # ------------------------------------------------------------
  - sa-longhorn.yaml

  # ------------------------------------------------------------
  # TLS and Certificate Management
  # ------------------------------------------------------------
  - cert-longhorn.yaml

  # ------------------------------------------------------------
  # Secrets Management (Vault CSI)
  # ------------------------------------------------------------
  - spc-s3.yaml
  - job-s3.yaml

  # ------------------------------------------------------------
  # Ingress and Exposure
  # ------------------------------------------------------------
  - ir-longhorn.yaml
