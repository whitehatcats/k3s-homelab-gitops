# ============================================================
# Kustomization - Velero S3 + CoreDNS Configuration
# ============================================================

resources:
  # ------------------------------------------------------------
  # Vault-Backed SecretProviderClass for Velero
  # ------------------------------------------------------------
  - spc-velero-s3.yaml

  # ------------------------------------------------------------
  # CoreDNS ConfigMap (Forward to Pi-hole)
  # ------------------------------------------------------------
  - cm-coredns-pihole.yaml
