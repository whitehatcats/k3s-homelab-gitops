# ============================================================
# Secrets Store CSI Driver — Minimal Values (Daniel's Config)
# ============================================================

syncSecret:
  enabled: true        # ✅ Sync secrets from Vault to Kubernetes Secrets

enableSecretRotation: false  # ✅ Disable automatic secret rotation (manual refresh only)

linux:
  enabled: true        # ✅ Enable Linux DaemonSet (your cluster runs Linux nodes)
