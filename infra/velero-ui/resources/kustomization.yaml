# ============================================================
# Kustomization - Velero UI Components
# ============================================================

resources:
  # ------------------------------------------------------------
  # Vault-Backed SecretProviderClasses
  # ------------------------------------------------------------
  - spc-velero-ui.yaml
  - spc-velero-ui-auth.yaml

  # ------------------------------------------------------------
  # Certificate and IngressRoute
  # ------------------------------------------------------------
  - cert-velero-ui.yaml
  - ir-velero-ui.yaml
