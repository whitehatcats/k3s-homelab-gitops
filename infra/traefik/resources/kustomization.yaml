# ============================================================
# Kustomization — Traefik (Ingress Controller Resources)
# ============================================================

resources:
  # ------------------------------------------------------------
  # Service Account
  # ------------------------------------------------------------
  - sa-traefik.yaml

  # ------------------------------------------------------------
  # TLS Certificate (cert-manager)
  # ------------------------------------------------------------
  - cert-traefik.yaml

  # ------------------------------------------------------------
  # IngressRoute (Traefik Dashboard)
  # ------------------------------------------------------------
  - ir-traefik.yaml
