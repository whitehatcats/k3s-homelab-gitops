# ============================================================
# Kustomization — Hubble Dashboard (Cilium)
# ============================================================

resources:
  # ------------------------------------------------------------
  # TLS Certificate
  # ------------------------------------------------------------
  - cert-hubble.yaml

  # ------------------------------------------------------------
  # IngressRoute (Traefik)
  # ------------------------------------------------------------
  - ir-hubble.yaml
