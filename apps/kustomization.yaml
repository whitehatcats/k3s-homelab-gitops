# ============================================================
# Argo CD - Applications (App of Apps)
# ============================================================

resources:
  # ----------------------------------------------------------
  # Core Applications
  # ----------------------------------------------------------
  - mariadb/app.yaml
  - phpmyadmin/app.yaml

  # ----------------------------------------------------------
  # WordPress Environments
  # ----------------------------------------------------------
  - wordpress/overlays/dev/app.yaml
  - wordpress/overlays/prod/app.yaml
